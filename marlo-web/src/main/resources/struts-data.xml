<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC 
"-//Apache Software Foundation//DTD Struts Configuration 2.5//EN" 
  "http://struts.apache.org/dtds/struts-6.0.dtd">
<struts>
    <!-- Add addition packages and configuration here. -->
    <package name="data" namespace="/data" extends="marlo-default">
        <action name="file" class="org.cgiar.ccafs.marlo.action.downloads.DownloadFileAction">
            <result name="success" type="stream">
                <param name="contentType">application/octet-stream</param>
                <param name="inputName">fileInputStream</param>
                <param name="contentDisposition">attachment;filename=${filename}</param>
                <param name="bufferSize">1024</param>
                <param name="edit">${edit}</param>
                <param name="phaseID">${phaseID}</param>
                <param name="projectID">${projectID}</param>
            </result>
            <result name="error" type="redirectAction">
                /WEB-INF/global/pages/error/404.ftl
                <param name="edit">${edit}</param>
                <param name="phaseID">${phaseID}</param>
                <param name="projectID">${projectID}</param>
            </result>
            <result name="login"  type="redirectAction">
                /WEB-INF/global/pages/error/401.ftl
                <param name="edit">${edit}</param>
                <param name="phaseID">${phaseID}</param>
                <param name="projectID">${projectID}</param>
            </result>
        </action>

        <!-- Homepage banner image (ENH-HOMEPAGE-BANNER-001). Dynamic contentType: the stored file may be a
             PNG, a JPEG or an SVG. No default-image fallback on error, unlike globalUnitLogo above. -->
        <action name="homepageBannerImage" class="org.cgiar.ccafs.marlo.action.downloads.DownloadHomepageBannerImageAction">
            <result name="success" type="stream">
                <param name="contentType">${contentType}</param>
                <param name="inputName">fileInputStream</param>
                <param name="bufferSize">1024</param>
            </result>
            <result name="error" type="redirectAction">
                /WEB-INF/global/pages/error/404.ftl
            </result>
        </action>

        <action name="globalUnitLogo" class="org.cgiar.ccafs.marlo.action.downloads.DownloadGlobalUnitLogoAction">
            <result name="success" type="stream">
                <param name="contentType">image/png</param>
                <param name="inputName">fileInputStream</param>
                <param name="bufferSize">1024</param>
            </result>
            <result name="error" type="redirectAction">
                /WEB-INF/global/pages/error/404.ftl
                <param name="edit">${edit}</param>
                <param name="phaseID">${phaseID}</param>
                <param name="projectID">${projectID}</param>
            </result>
        </action>


    </package>

    <package name="downloads" namespace="/downloads" extends="data" />

</struts>

