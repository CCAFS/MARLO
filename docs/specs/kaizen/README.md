Scaffolding only — this directory holds one kaizen entry file per spec, written by the `kaizen` skill's Record phase. This README is never a kaizen entry and never counts toward a pending-item count.
